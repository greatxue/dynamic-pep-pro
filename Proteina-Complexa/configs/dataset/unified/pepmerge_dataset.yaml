# PepMerge Dataset Config
# 基于 configs/dataset/unified/ted_dimers.yaml 修改
#
# 数据情况（PepMerge_release）：
#   肽长度：min=0, max=25, median=10
#   受体长度：min=14, max=3740, median=254
#   有效复合物数：10,223（有 receptor_merge.pdb 的）
#   输入文件：receptor_merge.pdb（肽链 + 完整受体，两链合一）

datamodule:
  _target_: proteinfoundation.datasets.structure_data.StructureDataModule

  # [TODO] 改成你的 CSV 文件绝对路径
  # CSV 需要两列：example_id 和 path（receptor_merge.pdb 的绝对路径）
  metadata_file: /path/to/pepmerge.csv  # → 改这里

  base_dir: ""

  # [TODO] 根据你的 GPU 显存调整，原值 6
  batch_size: 6  # → 肽链短可适当加大，如 16

  num_workers: 16
  train_split: 0.99
  path_column: path
  id_column: example_id
  pin_memory: true

  atom37_transforms:
    - _target_: proteinfoundation.datasets.transforms.CoordsToNanometers
    - _target_: proteinfoundation.datasets.transforms.GlobalRotationTransform

    # No TEDDimerTransform needed - files are already pre-merged
    - _target_: proteinfoundation.datasets.transforms.CroppingTransform2
      crop_size: 500
      interface_threshold: 8.0
      dist_mode: "bb_ca"
      data_mode: "all-atom"

      # [TODO] 原值 1，改为 3，过滤掉 0-2 残基的无效极短肽
      binder_min_length: 1  # → 改为 3

      # [TODO] 原值 250，改为 30（PepMerge 肽最长 25 残基）
      binder_max_length: 250  # → 改为 30

      binder_padding_length: 0

      # [TODO] 原值 50，改为 14（PepMerge 受体最短 14 残基）
      target_min_length: 50  # → 改为 14

      target_spatial_crop_threshold: 15.0
      max_num_target_chains: -1

    - _target_: proteinfoundation.datasets.transforms.ContactTransform
      cutoff: 8.0
      min_perc: 0.0
      max_perc: 0.2
      contact_prob: 1.0
      contact_dropout_prob: 0.5
      hotspot_dropout_prob: 0.5
    - _target_: proteinfoundation.datasets.transforms.OpenFoldFrameTransform
    - _target_: proteinfoundation.datasets.transforms.ChainBreakPerResidueTransform
    - _target_: proteinfoundation.datasets.transforms.CenteringTransform
      center_mode: "target"
      data_mode: "all-atom"
    - _target_: proteinfoundation.datasets.transforms.ExtractTargetCoordinatesTransform
      compact_mode: true
    - _target_: proteinfoundation.datasets.transforms.FilterTargetResiduesTransform
